# SafeZone Backend

Flask backend for SafeZone MVP.

## Endpoints
- `GET /health` simple health check.
- `POST /analyze` placeholder (will implement rule-based classifier).

## Dev Run
```bash
pip install -r requirements.txt
python app.py
```
